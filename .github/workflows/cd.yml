name: CD workflow

on: 
  push:
    branches:
      - main

jobs:
  cd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: install
        run: npm ci

      - name: build
        run: npm run build

      - name: deploy
        run: npm run deploy